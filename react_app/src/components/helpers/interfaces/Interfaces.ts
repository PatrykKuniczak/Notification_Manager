export interface IInitialValues {
    title: string
    description: string
    notificationDate: string
    important: boolean
}

export interface Task {
    id: number
    title: string
    description: string
    important: boolean
}